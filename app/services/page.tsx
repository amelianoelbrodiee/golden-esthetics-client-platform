import type{Metadata}from"next";import{ServiceCard}from"../components/ServiceCard";import{serviceCategories,services}from"../data/services";export const metadata:Metadata={title:"Services",description:"Explore Golden Esthetics facials, brows, lashes, waxing, and add-ons."};export default function Page(){return <><section className="page-hero shell"><p className="eyebrow">The Golden menu</p><h1>Services made<br/><em>for your glow.</em></h1><p>Explore facials, brows, lashes, waxing, and thoughtful add-ons.</p></section><div className="category-nav shell">{serviceCategories.map(c=><a key={c} href={`#${c.toLowerCase().replace(/[^a-z]+/g,"-")}`}>{c}</a>)}</div>{serviceCategories.map(c=>{const list=services.filter(x=>x.category===c);return <section className="section shell menu-section" id={c.toLowerCase().replace(/[^a-z]+/g,"-")} key={c}><div className="menu-heading"><p className="eyebrow">Golden Esthetics</p><h2>{c}</h2><span>{list.length} services</span></div><div className="service-grid">{list.map((x,i)=><ServiceCard key={x.id} service={x} index={i}/>)}</div></section>})}</>}
