"use client";import{useState}from"react";import{trackEvent}from"../lib/analytics";export function GoldenList(){const[status,setStatus]=useState("");const[busy,setBusy]=useState(false);async function submit(data:FormData){setBusy(true);const r=await fetch("/api/newsletter",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({firstName:data.get("firstName"),email:data.get("email"),source:"homepage"})});const body=await r.json();setStatus(body.message||body.error);setBusy(false);if(r.ok)trackEvent("golden_list_signup",{source:"homepage",demo:Boolean(body.demo)})}return <section className="golden-list"><div className="shell golden-list-inner"><div><p className="eyebrow">The Golden List</p><h2>A little glow in your inbox.</h2><p>Skincare tips, appointment openings & Golden Esthetics updates—without blowing up your inbox.</p></div><form action={submit}><input name="firstName" placeholder="First name · optional" autoComplete="given-name"/><input required type="email" name="email" placeholder="Email address" autoComplete="email"/><button className="button button-primary" disabled={busy}>{busy?"Joining…":"Join the list ✦"}</button>{status&&<small role="status">{status}</small>}</form></div></section>}
