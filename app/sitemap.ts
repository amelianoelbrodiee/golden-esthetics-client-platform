import type{MetadataRoute}from"next";export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_SITE_URL||"https://golden-esthetics-101699.square.site";return["","/services","/find-my-facial","/build-my-appointment","/gallery","/about","/faq","/contact","/privacy"].map((path,index)=>({url:`${base}${path}`,changeFrequency:index===0?"weekly":"monthly",priority:index===0?1:["/services","/find-my-facial"].includes(path)?.9:.6}))}
