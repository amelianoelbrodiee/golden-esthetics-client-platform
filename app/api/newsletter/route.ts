import{cleanText,isEmail}from"../../lib/forms";import{getSupabasePublicClient,isSupabaseConfigured}from"../../lib/supabase/server";
export async function POST(request:Request){try{const body=await request.json();const email=cleanText(body.email,254).toLowerCase();const firstName=cleanText(body.firstName,80);if(!isEmail(email))return Response.json({error:"Enter a valid email address."},{status:400});if(!isSupabaseConfigured())return Response.json({ok:true,demo:true,message:"Demo mode: your signup was accepted but not stored."});const{error}=await getSupabasePublicClient()!.from("newsletter_subscribers").insert({email,first_name:firstName||null,source:cleanText(body.source,80)||"website",active:true});if(error&&error.code!=="23505")throw error;return Response.json({ok:true,demo:false,message:"You’re on the Golden List ✦"})}catch{return Response.json({error:"Signup is unavailable right now. Please try again."},{status:500})}}
