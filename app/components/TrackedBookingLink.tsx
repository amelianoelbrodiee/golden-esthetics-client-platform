"use client";import type{ReactNode}from"react";import{trackEvent}from"../lib/analytics";export function TrackedBookingLink({href,serviceId,className,children}:{href:string;serviceId?:string;className?:string;children:ReactNode}){return <a className={className} href={href} target="_blank" rel="noreferrer" onClick={()=>trackEvent("booking_clicked",{service_id:serviceId??"general"})}>{children}</a>}
